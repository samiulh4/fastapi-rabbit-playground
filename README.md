"# fastapi-rabbit-playground" 
this is a playground for learning FastAPI and RabbitMQ. It includes a simple FastAPI application that allows you to create users and send messages to RabbitMQ. The application uses MongoDB as the database to store user information.

# pip freeze > requirements.txt
# pip install -r requirements.txt
# uvicorn app.main:app --reload
